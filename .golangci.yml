version: "2"
linters:
  default: standard
  enable:
    - errorlint
    - misspell
    - unconvert
    - revive
  settings:
    revive:
      rules:
        - name: exported
formatters:
  enable:
    - gofmt
